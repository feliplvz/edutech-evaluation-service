# Database Configuration
# Railway PostgreSQL connection details
DATABASE_URL=jdbc:postgresql://your-host:port/database_name
DATABASE_USERNAME=your_username
DATABASE_PASSWORD=your_password

# Application Configuration
SERVER_PORT=8083

# Optional: For local development
# DATABASE_URL=jdbc:postgresql://localhost:5432/evaluation_db
# DATABASE_USERNAME=postgres
# DATABASE_PASSWORD=password

# Instructions:
# 1. Copy this file to .env
# 2. Replace the placeholder values with your actual database credentials
# 3. The .env file will be ignored by git (see .gitignore)
